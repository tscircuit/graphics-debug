<svg width="200" height="200" viewBox="0 0 200 200" xmlns="http://www.w3.org/2000/svg"><rect width="100%" height="100%" fill="white"/><g><polyline data-points="5,-4 5,10" data-type="line" data-label="bottom" points="117.14285714285714,160 117.14285714285714,40.000000000000014" fill="none" stroke="blue" stroke-width="8.571428571428571"/><text x="122.14285714285714" y="155" font-family="sans-serif" font-size="12" fill="blue">bottom</text></g><g><polyline data-points="-4,5 10,5" data-type="line" data-label="top" points="39.99999999999999,82.85714285714286 160,82.85714285714286" fill="none" stroke="black" stroke-width="8.571428571428571"/><text x="44.99999999999999" y="77.85714285714286" font-family="sans-serif" font-size="12" fill="black">top</text></g><g id="crosshair" style="display: none"><line id="crosshair-h" y1="0" y2="200" stroke="#666" stroke-width="0.5"/><line id="crosshair-v" x1="0" x2="200" stroke="#666" stroke-width="0.5"/><text id="coordinates" font-family="monospace" font-size="12" fill="#666"></text></g><script><![CDATA[
              document.currentScript.parentElement.addEventListener('mousemove', (e) => {
                const svg = e.currentTarget;
                const rect = svg.getBoundingClientRect();
                const x = e.clientX - rect.left;
                const y = e.clientY - rect.top;
                const crosshair = svg.getElementById('crosshair');
                const h = svg.getElementById('crosshair-h');
                const v = svg.getElementById('crosshair-v');
                const coords = svg.getElementById('coordinates');
                
                crosshair.style.display = 'block';
                h.setAttribute('x1', '0');
                h.setAttribute('x2', '200');
                h.setAttribute('y1', y);
                h.setAttribute('y2', y);
                v.setAttribute('x1', x);
                v.setAttribute('x2', x);
                v.setAttribute('y1', '0');
                v.setAttribute('y2', '200');

                // Calculate real coordinates using inverse transformation
                const matrix = {"a":8.571428571428571,"c":0,"e":74.28571428571428,"b":0,"d":-8.571428571428571,"f":125.71428571428572};
                // Manually invert and apply the affine transform
                // Since we only use translate and scale, we can directly compute:
                // x' = (x - tx) / sx
                // y' = (y - ty) / sy
                const sx = matrix.a;
                const sy = matrix.d;
                const tx = matrix.e; 
                const ty = matrix.f;
                const realPoint = {
                  x: (x - tx) / sx,
                  y: (y - ty) / sy // Flip y back since we used negative scale
                }
                
                coords.textContent = `(${realPoint.x.toFixed(2)}, ${realPoint.y.toFixed(2)})`;
                coords.setAttribute('x', (x + 5).toString());
                coords.setAttribute('y', (y - 5).toString());
              });
              document.currentScript.parentElement.addEventListener('mouseleave', () => {
                document.currentScript.parentElement.getElementById('crosshair').style.display = 'none';
              });
            ]]></script></svg>